apiVersion: 1

providers:
  - name: "Default"
    orgId: 1
    folder: "Telemetry Live"
    type: file
    updateIntervalSeconds: 50
    options:
      path: /var/lib/grafana/dashboards/telemetry_live
  - name: "Default_2"
    orgId: 1
    folder: "Telemetry Stored"
    type: file
    updateIntervalSeconds: 50
    options:
      path: /var/lib/grafana/dashboards/telemetry_stored

